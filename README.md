spring-batch-cluster
====================

simple solution to running batch in a cluster of batch servers

SubProjects include;
- write-behind for the Batch Job Repository
- HA for batch in a cluster (automatic stop and failover of executing jobs)
